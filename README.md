# SudokuSolver
A Common Lisp Prog to solve sudokus

## 主函数

### 参数

二维数组

算法：生成9个list，表示1到9，9个数的坐标。循环遍历每个数，在各个数内，计算出剩余行、列、格。

分解：

主函数需要生成9个结构，每个结构包含已经计算的该数坐标、该数还未确定的行、列、格

  遍历数独每个值，若为空，不操作；若不为空，取该数x，存入数组[x-1]位置的行和列，取行和列